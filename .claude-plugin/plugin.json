{
  "name": "techwriter-mcp",
  "version": "0.1.0",
  "description": "Writes your docs, PR descriptions, and issues from facts, so the coding agent never has to. Ships the techwriter MCP server, the writing-prose workflow skill, and /techwriter-init.",
  "author": { "name": "Wes Roberts" },
  "repository": "https://github.com/paraspace-dev/techwriter-mcp",
  "license": "MIT",
  "keywords": ["documentation", "writing", "mcp"],
  "mcpServers": {
    "techwriter": {
      "command": "npx",
      "args": ["-y", "techwriter-mcp@latest"],
      "env": {
        "OPENAI_API_KEY": "${user_config.openai_api_key}"
      }
    }
  },
  "userConfig": {
    "openai_api_key": {
      "type": "string",
      "title": "OpenAI API key",
      "description": "The techwriter server calls an OpenAI model to write prose. Create a key at https://platform.openai.com/api-keys.",
      "sensitive": true,
      "required": true
    }
  }
}
